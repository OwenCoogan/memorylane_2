export default class Post {
  constructor(userId,name,description,geolocation) {
    this.userId = userId,
    this.name = name,
    this.description = description;
    this.geolocation = geolocation;
  }
}
